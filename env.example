# Copy to .env and fill (do not commit .env).
# Binance USDT-M API — required for live; dry_run may still need keys depending on exchange tier.

BINANCE_API_KEY=
BINANCE_API_SECRET=

# Optional: if you inject API server secrets outside config JSON
# FT_API_JWT_SECRET=
# FT_API_UI_PASSWORD=
